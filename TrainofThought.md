Student
    Attributes: name, scores (0 - 100) *stored as NumPy array*
        Exception: scores outside range will be rejected and raise an error
        Exception: once student record is built, raise an error when an attempt
            to add another record is made
        clean data through pandas
          